//update the prisma schema so that it includes the new table
//it seems that using a table with a vector as json, does not work, because there is no easy way
//calculating the closest vector on that form, you will need to use (option a) vector data type
//or use the (possibly existant) (option b) native functionality to create vector embedding on supabase
